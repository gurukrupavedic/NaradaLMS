node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables (contains sensitive data like passwords)
.env
.env.local

# Development logs
dev.log
dev.error.log

# Uploaded media files (audio, video, etc.)
# These should not be in version control - use cloud storage in production
uploads/
!uploads/.gitkeep
