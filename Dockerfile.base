# Base Dockerfile for Narada LMS
ARG NODE_VERSION=20.16.11

FROM node:${NODE_VERSION}-alpine AS base
RUN apk add --no-cache libc6-compat
RUN npm install -g turbo
WORKDIR /app

# Enable pnpm if we switch later, but sticking to npm for now as per repo
# RUN npm install -g pnpm
